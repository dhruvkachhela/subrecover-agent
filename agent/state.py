# How this works:
# Defines the state schema used by LangGraph to track recovery lifecycle
# for each failed subscription attempt across multiple agent nodes.

"""LangGraph state schema for recovery workflow."""
